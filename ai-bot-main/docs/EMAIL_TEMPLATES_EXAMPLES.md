# Примеры HTML-шаблонов для писем

Этот файл содержит примеры HTML-шаблонов для использования в админ-панели `/admin/email-templates`.

## Как использовать

1. Перейдите в админ-панель: `/admin/email-templates`
2. Выберите нужный шаблон
3. Скопируйте HTML из соответствующего раздела ниже
4. Вставьте в поле "Сообщение" (message) для нужного языка
5. Используйте плейсхолдеры `{{key}}` для подстановки динамических значений

## Доступные плейсхолдеры

- `{{service_name}}` - название сервиса
- `{{amount}}` - сумма платежа
- `{{email}}` - email пользователя
- `{{url}}` - URL для ссылок (например, для сброса пароля)

---

## 1. subscription_activated (Активация подписки)

### Тема письма (title):
```
Подписка активирована - {{service_name}}
```

### Сообщение (message):
```html
<div class="badge badge-success">✓ Подписка активирована</div>

<p>Поздравляем! Ваша подписка на <strong>{{service_name}}</strong> успешно активирована.</p>

<div class="info-box">
    <h3>Детали подписки</h3>
    <div class="info-row">
        <span class="info-label">Сервис:</span>
        <span class="info-value">{{service_name}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Статус:</span>
        <span class="info-value">Активна</span>
    </div>
</div>

<p>Теперь вы можете использовать все возможности сервиса. Если у вас возникнут вопросы, наша служба поддержки всегда готова помочь.</p>

<p>Спасибо за выбор SubCloudy!</p>
```

---

## 2. payment_confirmation (Подтверждение оплаты)

### Тема письма (title):
```
Оплата подтверждена
```

### Сообщение (message):
```html
<div class="badge badge-success">✓ Оплата подтверждена</div>

<p>Спасибо за ваш платеж! Мы успешно получили оплату за вашу подписку.</p>

<div class="info-box">
    <h3>Детали платежа</h3>
    <div class="info-row">
        <span class="info-label">Сумма:</span>
        <span class="info-value">{{amount}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Статус:</span>
        <span class="info-value">Оплачено</span>
    </div>
</div>

<p>Ваша подписка продлена и активна. Вы можете продолжать пользоваться всеми услугами SubCloudy.</p>

<p>Если у вас есть вопросы по платежу, пожалуйста, свяжитесь с нашей службой поддержки.</p>
```

---

## 3. subscription_expiring (Истечение подписки)

### Тема письма (title):
```
Подписка скоро истечет - {{service_name}}
```

### Сообщение (message):
```html
<div class="badge badge-warning">⚠ Подписка скоро истечет</div>

<p>Уважаемый пользователь!</p>

<p>Ваша подписка на <strong>{{service_name}}</strong> истечет через 3 дня.</p>

<div class="info-box">
    <h3>Информация о подписке</h3>
    <div class="info-row">
        <span class="info-label">Сервис:</span>
        <span class="info-value">{{service_name}}</span>
    </div>
    <div class="info-row">
        <span class="info-label">Дата истечения:</span>
        <span class="info-value">Через 3 дня</span>
    </div>
</div>

<p>Чтобы продолжить пользоваться услугами, пожалуйста, продлите подписку до истечения срока действия.</p>

<p>Если у вас включено автоматическое продление, подписка будет продлена автоматически при наличии средств на вашем счете.</p>

<p>Спасибо за использование SubCloudy!</p>
```

---

## 4. reset_password (Сброс пароля)

### Тема письма (title):
```
Сброс пароля для вашего аккаунта
```

### Сообщение (message):
```html
<div class="badge badge-info">🔐 Сброс пароля</div>

<p>Вы запросили сброс пароля для вашего аккаунта SubCloudy.</p>

<p>Для создания нового пароля нажмите на кнопку ниже:</p>

{{button}}

<p style="color: #999; font-size: 14px; margin-top: 20px;">
    Если вы не запрашивали сброс пароля, просто проигнорируйте это письмо. Ваш пароль останется без изменений.
</p>

<p style="color: #999; font-size: 12px; margin-top: 15px;">
    Ссылка действительна в течение 60 минут.
</p>
```

**Примечание:** Для `reset_password` используется специальный шаблон `emails.reset-password.blade.php`, который автоматически заменяет `{{button}}` на кнопку с ссылкой.

---

## Доступные CSS классы

В шаблонах можно использовать следующие CSS классы:

- `.badge` - базовый класс для бейджей
- `.badge-success` - зеленый бейдж (успех)
- `.badge-info` - синий бейдж (информация)
- `.badge-warning` - желтый бейдж (предупреждение)
- `.info-box` - информационный блок с градиентным фоном
- `.info-row` - строка информации
- `.info-label` - метка информации
- `.info-value` - значение информации
- `.button` - стилизованная кнопка (автоматически добавляется в base.blade.php)

Все стили определены в `resources/views/emails/base.blade.php`.

