# Отчет о багах функционала техподдержки

**Дата проверки:** 2025-12-24  
**Сайт:** https://subcloudy.com/  
**Языки проверены:** English, Русский

---

## 🔴 КРИТИЧЕСКИЕ БАГИ

### БАГ #1: Модальное окно закрывается при клике на кнопки каналов
**Приоритет:** КРИТИЧЕСКИЙ  
**Воспроизводимость:** 100%  
**Языки:** Все языки

**Описание:**
При клике на кнопки выбора канала ("Чат на сайте" или "Telegram") модальное окно закрывается вместо того, чтобы перейти к следующему шагу (ввод email для неавторизованных пользователей или инициализация чата для авторизованных).

**Шаги для воспроизведения:**
1. Открыть сайт https://subcloudy.com/
2. Нажать на кнопку "Поддержка" (плавающая кнопка в правом нижнем углу)
3. В открывшемся модальном окне нажать на кнопку "Чат на сайте" или "Telegram"
4. **Результат:** Модальное окно закрывается

**Ожидаемое поведение:**
- Для неавторизованных пользователей: должен открыться шаг ввода email
- Для авторизованных пользователей: должен открыться чат или перенаправление в Telegram

**Фактическое поведение:**
- Модальное окно закрывается (`modalVisible: false`)
- Элементы остаются в DOM (поле email видно в snapshot, но модальное окно невидимо)

**Технические детали:**
- После клика: `modalVisible: false`, но `emailInputVisible: true`
- Это указывает на то, что `isVisible.value` становится `false`, но элементы не удаляются из DOM
- Возможно, проблема в обработчике `handleChannelClick` или в логике `handleOverlayClick`

---

### БАГ #2: Элементы модального окна остаются в DOM после закрытия
**Приоритет:** ВЫСОКИЙ  
**Воспроизводимость:** 100%  
**Языки:** Все языки

**Описание:**
После закрытия модального окна (особенно после клика на кнопки каналов) элементы модального окна остаются в DOM, что может вызывать проблемы с производительностью и конфликты в интерфейсе.

**Шаги для воспроизведения:**
1. Открыть модальное окно поддержки
2. Нажать на кнопку канала
3. Проверить DOM через DevTools
4. **Результат:** Элементы модального окна (input[type="email"], кнопки) остаются в DOM

**Ожидаемое поведение:**
- Все элементы модального окна должны быть удалены из DOM после закрытия

**Фактическое поведение:**
- Элементы остаются в DOM, но модальное окно невидимо

---

## 🟡 СРЕДНИЕ БАГИ

### БАГ #3: Кнопка "Начать чат" не найдена после ввода email
**Приоритет:** СРЕДНИЙ  
**Воспроизводимость:** Требует дополнительной проверки  
**Языки:** Все языки

**Описание:**
После ввода email в поле ввода кнопка "Начать чат" не найдена в DOM. Это может быть связано с тем, что модальное окно закрылось до того, как пользователь успел ввести email.

**Шаги для воспроизведения:**
1. Открыть модальное окно поддержки
2. Нажать на кнопку канала
3. Ввести email в поле ввода
4. Попытаться найти кнопку "Начать чат"
5. **Результат:** Кнопка не найдена (`submitBtnExists: false`)

**Ожидаемое поведение:**
- Кнопка "Начать чат" должна быть видна и доступна после ввода валидного email

**Фактическое поведение:**
- Кнопка не найдена в DOM

**Примечание:** Этот баг может быть следствием БАГ #1

---

## 🟢 НИЗКИЕ БАГИ / УЛУЧШЕНИЯ

### БАГ #4: Placeholder email не локализован
**Приоритет:** НИЗКИЙ  
**Воспроизводимость:** 100%  
**Языки:** Русский

**Описание:**
На русском языке placeholder для поля email показывает "Email" вместо локализованного текста.

**Шаги для воспроизведения:**
1. Переключить язык на русский
2. Открыть модальное окно поддержки
3. Нажать на кнопку канала
4. Проверить placeholder поля email
5. **Результат:** `emailPlaceholder: "Email"` (должно быть "your@email.com" или локализованный текст)

**Ожидаемое поведение:**
- Placeholder должен быть локализован согласно текущему языку

**Фактическое поведение:**
- Placeholder показывает "Email" на всех языках

---

### БАГ #5: Отсутствует визуальная обратная связь при клике на кнопки каналов
**Приоритет:** НИЗКИЙ  
**Воспроизводимость:** 100%  
**Языки:** Все языки

**Описание:**
При клике на кнопки каналов нет визуальной обратной связи (например, индикации загрузки), что может создавать впечатление, что ничего не происходит.

**Ожидаемое поведение:**
- Должна быть визуальная обратная связь (индикация загрузки, изменение состояния кнопки)

**Фактическое поведение:**
- Нет визуальной обратной связи

---

## ✅ РАБОТАЕТ КОРРЕКТНО

1. **Локализация:** Переключение языков работает корректно, все тексты переводятся
2. **Кнопка закрытия:** Имеет правильный `aria-label` на всех языках ("Закрыть" на русском, "Close" на английском)
3. **Заголовок модального окна:** Корректно локализован ("Служба поддержки" на русском, "Support Service" на английском)
4. **Кнопки каналов:** Отображаются корректно с правильными текстами
5. **Валидация email:** Регулярное выражение работает корректно

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (КРИТИЧЕСКИЙ):
1. **Исправить БАГ #1:** Модальное окно не должно закрываться при клике на кнопки каналов
   - Проверить обработчик `handleChannelClick`
   - Убедиться, что `event.stopPropagation()` и `event.preventDefault()` вызываются корректно
   - Проверить логику `handleOverlayClick` - возможно, она срабатывает неправильно
   - Добавить дополнительную защиту от закрытия на шагах 'channel' и 'email'

### Приоритет 2 (ВЫСОКИЙ):
2. **Исправить БАГ #2:** Убедиться, что элементы удаляются из DOM после закрытия
   - Проверить использование `Teleport` и `Transition`
   - Убедиться, что `v-if="isVisible"` работает корректно

### Приоритет 3 (СРЕДНИЙ):
3. **Исправить БАГ #3:** Проверить отображение кнопки "Начать чат"
   - Убедиться, что кнопка отображается после перехода на шаг 'email'
   - Проверить условие `v-if` для кнопки

### Приоритет 4 (НИЗКИЙ):
4. **Исправить БАГ #4:** Локализовать placeholder email
5. **Исправить БАГ #5:** Добавить визуальную обратную связь при клике на кнопки каналов

---

## 🔍 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Проверенные элементы:
- ✅ Кнопка поддержки открывает модальное окно
- ✅ Модальное окно отображается с правильным заголовком
- ✅ Кнопки каналов отображаются корректно
- ❌ Клик на кнопки каналов закрывает модальное окно (БАГ)
- ✅ Локализация работает корректно
- ✅ Кнопка закрытия имеет правильный aria-label
- ❌ Элементы остаются в DOM после закрытия (БАГ)

### Сетевые запросы:
- Нет запросов к API при клике на кнопки каналов (что правильно, так как модальное окно закрывается до инициализации)

### Консольные ошибки:
- `[ERROR] Failed to load resource: the server responded with a status of 404 (Not Found) @ https://subcloudy.com/assets/logo-BkC0PaDI.webp` - не критично, это проблема с ресурсом

---

## 📝 РЕКОМЕНДАЦИИ

1. **Немедленно исправить БАГ #1** - это критическая проблема, которая блокирует весь функционал техподдержки
2. Добавить логирование для отладки событий клика
3. Добавить unit-тесты для обработчиков событий
4. Рассмотреть использование `@click.stop.prevent` на всех интерактивных элементах внутри модального окна
5. Проверить использование `Teleport` - возможно, проблема в том, что элементы не удаляются из DOM

---

**Статус:** Требуется срочное исправление критических багов



