# 🚀 Quick Start Guide - SubCloudy Desktop

## Что создано?

Полнофункциональное Electron-приложение для безопасного доступа к premium-сервисам через SubCloudy.

### ✅ Реализовано

1. **Main Process** - управление приложением, безопасность, изоляция сессий
2. **Renderer Process** - Vue.js интерфейс с авторизацией и списком сервисов
3. **Security Manager** - полный контроль над безопасностью (DevTools блокировка, watermarks, мониторинг)
4. **Auth Manager** - интеграция с Laravel backend
5. **Service Manager** - запуск сервисов в изолированных BrowserView
6. **Backend Integration** - новые endpoints в Laravel для desktop
7. **Build Configuration** - готовая настройка для сборки под Windows/Mac/Linux

---

## 🏃 Быстрый старт (5 минут)

### Шаг 1: Backend (Laravel)

```bash
# Перейдите в папку backend
cd D:\project\Subcloudy\ai-bot-main

# Если backend не запущен, запустите
php artisan serve

# Backend будет доступен на http://127.0.0.1:8000
```

### Шаг 2: Desktop App

```bash
# Перейдите в папку desktop приложения
cd D:\project\Subcloudy\subcloudy-desktop

# Установите зависимости (только первый раз)
npm install

# Создайте .env файл
echo "API_URL=http://127.0.0.1:8000/api" > .env
echo "NODE_ENV=development" >> .env

# Запустите приложение
npm run dev
```

### Шаг 3: Тестирование

1. Приложение откроется автоматически
2. Войдите используя учетные данные с subcloudy.com
3. Выберите любой активный сервис
4. Сервис откроется в защищенном изолированном окне

---

## 📋 Что проверить?

### ✅ Безопасность

1. **DevTools блокировка**
   - Нажмите F12 → ничего не должно произойти
   - Попробуйте Ctrl+Shift+I → заблокировано
   - Правый клик → меню не появляется

2. **Cookie защита**
   - Попробуйте выполнить `document.cookie` в консоли → невозможно
   - Network tab недоступен

3. **Watermark**
   - Посмотрите на прозрачный текст с User ID
   - Попробуйте удалить через инспектор → автоматически восстанавливается

4. **Автозакрытие**
   - Оставьте сервис открытым на 30+ минут без активности
   - Сессия должна автоматически закрыться

### ✅ Функциональность

1. **Авторизация**
   - Успешный вход
   - Сохранение токена
   - Корректный logout

2. **Список сервисов**
   - Отображаются только активные подписки пользователя
   - Иконки и названия корректны

3. **Запуск сервиса**
   - Быстрое открытие
   - Автологин в сервис
   - Полноценная работа

---

## 📁 Структура файлов

```
subcloudy-desktop/
├── src/
│   ├── main/
│   │   ├── index.ts           # Точка входа Electron
│   │   ├── auth.ts            # Менеджер аутентификации
│   │   ├── security.ts        # Система безопасности ⭐
│   │   ├── services.ts        # Менеджер сервисов
│   │   └── windows/
│   │       └── mainWindow.ts  # Главное окно
│   ├── preload/
│   │   └── index.ts           # IPC мост (безопасная связь)
│   └── renderer/
│       └── src/
│           ├── main.ts        # Vue entry point
│           ├── App.vue        # Главный компонент
│           ├── router.ts      # Vue Router
│           ├── stores/        # Pinia stores
│           │   ├── auth.ts    # Состояние авторизации
│           │   └── services.ts# Состояние сервисов
│           └── pages/
│               ├── LoginPage.vue    # Страница входа
│               └── ServicesPage.vue # Список сервисов
├── package.json               # Зависимости и скрипты
├── vite.config.ts            # Конфиг Vite
├── tsconfig.json             # Конфиг TypeScript
├── electron-builder.yml      # Конфиг сборки
└── README.md                 # Документация
```

---

## 🔧 Backend изменения

Добавлены файлы в Laravel проект:

```
ai-bot-main/
├── app/Http/Controllers/Api/
│   └── DesktopController.php      # ⭐ Новый контроллер
└── routes/
    └── api.php                     # ⭐ Добавлены роуты /api/desktop/*
```

### Новые API endpoints:

```
POST /api/desktop/auth          - Получение токена для desktop
POST /api/desktop/service-url   - Получение защищенной ссылки на сервис
GET  /api/desktop/my-services   - Список активных сервисов пользователя
POST /api/desktop/log           - Логирование активности
```

---

## 🎨 UI/UX

- **Дизайн**: Современный gradient-стиль с темной/светлой темой
- **Анимации**: Плавные переходы и hover-эффекты
- **Адаптивность**: Responsive grid для сервисов
- **Иконки**: Поддержка кастомных лого для каждого сервиса

---

## 🛠️ Полезные команды

```bash
# Разработка
npm run dev              # Запуск в dev режиме

# Сборка
npm run build           # Компиляция без упаковки
npm run dist            # Полная сборка для текущей ОС
npm run dist:win        # Сборка для Windows
npm run dist:mac        # Сборка для macOS
npm run dist:linux      # Сборка для Linux

# Отладка
npm run dev:renderer    # Только Vite dev server
npm run dev:main        # Только компиляция main process
```

---

## 🔐 Механизмы безопасности

### 1. Изоляция сессий
- Каждый сервис в отдельном partition
- Уникальная session для каждого запуска
- Автоочистка после закрытия

### 2. DevTools Prevention
```typescript
webPreferences: {
  devTools: false,  // Отключено на уровне API
  ...
}
```

### 3. Cookie Protection
```typescript
Object.defineProperty(document, 'cookie', {
  get: () => '',
  set: () => true,
  configurable: false
});
```

### 4. Keyboard Blocking
- F12 → заблокирован
- Ctrl+Shift+I/J/C → заблокированы
- Ctrl+U (view source) → заблокирован

### 5. Watermarking
```typescript
// Невидимый watermark с User ID
opacity: 0.03
z-index: 999999
```

### 6. Activity Monitoring
- Отслеживание навигации
- Тайм-аут неактивности (30 мин)
- Логирование в backend

---

## 📦 Сборка для production

### 1. Подготовка

```bash
# Обновите версию в package.json
{
  "version": "1.0.0"
}

# Убедитесь что все работает
npm run dev
```

### 2. Сборка

```bash
# Для Windows
npm run dist:win
# Результат: dist-electron/SubCloudy-Setup-1.0.0.exe

# Для всех платформ (если на Mac)
npm run dist:win
npm run dist:mac  
npm run dist:linux
```

### 3. Тестирование сборки

Установите собранное приложение и проверьте:
- ✅ Успешная установка
- ✅ Корректный запуск
- ✅ Работа всех функций
- ✅ Безопасность (DevTools недоступны)

---

## 🚨 Важные замечания

1. **API URL**: В production измените на реальный домен
   ```env
   API_URL=https://api.subcloudy.com/api
   ```

2. **CORS**: Настройте на backend для desktop приложения
   ```php
   'allowed_origins' => [
       'file://*',
       'capacitor://*'
   ]
   ```

3. **Code Signing**: Для production обязательно подпишите приложение

4. **Auto-updates**: Настройте сервер обновлений

5. **Иконки**: Замените placeholder иконки на реальные:
   - `resources/icons/icon.ico` (256x256 Windows)
   - `resources/icons/icon.icns` (512x512 macOS)
   - `resources/icons/icon.png` (512x512 Linux)

---

## 🎯 Следующие шаги

### Обязательно:
1. [ ] Создать настоящие иконки приложения
2. [ ] Настроить code signing
3. [ ] Протестировать на всех платформах
4. [ ] Настроить auto-update сервер

### Опционально:
1. [ ] Добавить тему оформления (light/dark toggle)
2. [ ] Добавить настройки приложения
3. [ ] Добавить историю запусков
4. [ ] Добавить уведомления
5. [ ] Добавить горячие клавиши

---

## 💡 Полезные ссылки

- [Electron Docs](https://www.electronjs.org/docs/latest)
- [Vue.js Guide](https://vuejs.org/guide/introduction.html)
- [electron-builder](https://www.electron.build/)
- [Pinia Docs](https://pinia.vuejs.org/)

---

## 🐛 Известные проблемы

**Windows Defender может заблокировать**
- Решение: Добавьте приложение в исключения или подпишите код

**macOS Gatekeeper**
- Решение: "Системные настройки → Безопасность → Открыть в любом случае"

**Linux permission denied**
- Решение: `chmod +x SubCloudy.AppImage`

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте консоль (в терминале откуда запустили `npm run dev`)
2. Проверьте backend логи (`storage/logs/laravel.log`)
3. Убедитесь что версии Node.js и npm актуальны
4. Попробуйте `rm -rf node_modules && npm install`

---

**Готово! Приложение полностью функционально и готово к тестированию! 🎉**


