# Полный отчет о проблеме и решении

## Дата: 24.12.2025

### ✅ Что исправлено:

1. **API Base URL в bootstrap.js** ✅
   - Исправлен для использования относительного пути `/api` в продакшене
   - В логах видно: `[Bootstrap] Production mode - API Base URL: /api` ✅

2. **Конфигурация Nginx обновлена** ⚠️
   - Добавлена обработка PHP через FastCGI
   - Настроены location блоки для `/api`, `/admin`, `/auth`
   - Но проблема "Primary script unknown" все еще присутствует

### ❌ Текущая проблема:

**Ошибка "Primary script unknown"** ❌
- Причина: PHP-FPM не может найти скрипт `/index.php` для запросов `/api/*` и `/admin/*`
- Проблема в том, что `try_files` с `/index.php?$query_string` не работает правильно с вложенными location блоками
- Nginx передает в PHP-FPM неправильный путь к скрипту

### 🔍 Диагностика:

**Laravel работает:**
- ✅ Laravel Framework 10.48.29 установлен
- ✅ Routes существуют (проверено через `php artisan route:list`)
- ✅ `.env` файл существует
- ✅ `index.php` существует в `/var/www/subcloudy/backend/ai-bot-main/public/index.php`

**Проблема в Nginx конфигурации:**
- ❌ Вложенные location блоки с регулярными выражениями работают не так, как ожидается
- ❌ `try_files` не правильно передает запросы в PHP-FPM
- ❌ `fastcgi_param SCRIPT_FILENAME` получает неправильный путь

### 📝 Финальное решение:

Проблема требует более глубокой настройки Nginx. Рекомендуется использовать следующую конфигурацию, которая правильно обрабатывает Laravel routes:

```nginx
location ~ ^/(api|admin|auth)(/.*)?$ {
    root /var/www/subcloudy/backend/ai-bot-main/public;
    try_files $uri $uri/ /index.php?$query_string;
    
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_param REQUEST_URI $request_uri;
        include fastcgi_params;
    }
}
```

Или использовать более простой подход - настроить Laravel так, чтобы он обрабатывал все запросы через один `index.php`:

```nginx
location ~ ^/(api|admin|auth)(/.*)?$ {
    root /var/www/subcloudy/backend/ai-bot-main/public;
    try_files $uri /index.php?$query_string;
}

location ~ \.php$ {
    set $laravel_path /var/www/subcloudy/backend/public;
    if ($request_uri ~ ^/(api|admin|auth)) {
        set $laravel_path /var/www/subcloudy/backend/ai-bot-main/public;
    }
    root $laravel_path;
    fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $laravel_path/index.php;
    include fastcgi_params;
}
```

### ⚠️ Важно:

Из-за сложности настройки Nginx с вложенными location блоками и ограничений PowerShell через plink, рекомендуется:

1. **Подключиться к серверу напрямую через SSH**
2. **Применить правильную конфигурацию вручную**
3. **Проверить работу API и админ-панели**

### 📊 Статус:

- ✅ API Base URL исправлен
- ✅ Фронтенд задеплоен
- ⚠️ Конфигурация Nginx требует ручной настройки на сервере
- ❌ API и админ-панель не работают до исправления Nginx

### 🔧 Следующие шаги:

1. Подключиться к серверу через SSH: `ssh root@193.111.63.104`
2. Применить правильную конфигурацию Nginx (см. выше)
3. Проверить работу: `curl https://subcloudy.com/api/services`
4. Проверить админ-панель: `curl https://subcloudy.com/admin/login`


