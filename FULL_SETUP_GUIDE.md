# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ЗАПУСКУ

## ⚠️ **ПРОБЛЕМА:**
При запуске `http://localhost:5173` отображается только прелоадер.

## 🔍 **ПРИЧИНА:**
Laravel backend НЕ ЗАПУЩЕН! Фронтенд не может подключиться к API.

---

## ✅ **РЕШЕНИЕ:**

### **Шаг 1: Запустить Laravel Backend**

**Вариант A: Через BAT файл (рекомендуется)**
1. Откройте проводник
2. Перейдите: `D:\project\Subcloudy\ai-bot-main`
3. Дважды кликните: `START_BACKEND.bat`
4. Должно появиться: `Laravel development server started: http://127.0.0.1:8000`

**Вариант B: Через командную строку**
```cmd
cd D:\project\Subcloudy\ai-bot-main
php artisan serve --host=127.0.0.1 --port=8000
```

**Проверка:**
Откройте в браузере: http://127.0.0.1:8000/api/services
Должен вернуться JSON с сервисами.

---

### **Шаг 2: Запустить Frontend**

**После запуска backend:**
1. Откройте проводник
2. Перейдите: `D:\project\Subcloudy\ai-bot-front-main`
3. Дважды кликните: `START_FRONTEND.bat`
4. Откроется: `http://localhost:5173`

---

### **Шаг 3: Обновить страницу**

1. Вернитесь в браузер
2. Обновите страницу: `http://localhost:5173`
3. Прелоадер должен исчезнуть!

---

## 📍 **ЧТО ДОЛЖНО БЫТЬ ЗАПУЩЕНО:**

✅ **Laravel Backend:** `http://127.0.0.1:8000`  
✅ **Vite Frontend:** `http://localhost:5173`  
✅ **Desktop App:** `subcloudy-desktop/START_APP.bat` (опционально)

---

## 🐛 **ЕСЛИ НЕ РАБОТАЕТ:**

### **Проблема 1: PHP не найден**
```cmd
cd D:\project\Subcloudy\ai-bot-main
php --version
```

Если ошибка "php is not recognized":
- Установите PHP или добавьте в PATH
- Используйте Laragon/XAMPP/OpenServer

### **Проблема 2: Laravel не запускается**
```cmd
cd D:\project\Subcloudy\ai-bot-main
composer install
php artisan key:generate
php artisan migrate
php artisan serve
```

### **Проблема 3: Порт 8000 занят**
```cmd
netstat -ano | findstr ":8000"
taskkill /PID <PID> /F
```

### **Проблема 4: Фронтенд показывает CORS ошибку**

Файл: `ai-bot-main/config/cors.php`
```php
'allowed_origins' => ['http://localhost:5173'],
'supports_credentials' => true,
```

---

## 🎯 **ПРАВИЛЬНЫЙ ПОРЯДОК ЗАПУСКА:**

1. **Сначала Backend** → `ai-bot-main/START_BACKEND.bat`
2. **Потом Frontend** → `ai-bot-front-main/START_FRONTEND.bat`
3. **Открыть браузер** → `http://localhost:5173`

---

## 📝 **БЫСТРЫЙ ЗАПУСК (для вас):**

Откройте **2 окна командной строки**:

**Окно 1 (Backend):**
```cmd
cd D:\project\Subcloudy\ai-bot-main
START_BACKEND.bat
```

**Окно 2 (Frontend):**
```cmd
cd D:\project\Subcloudy\ai-bot-front-main
START_FRONTEND.bat
```

**Ждите сообщение:**
```
✓ ready in XXX ms
➜ Local: http://localhost:5173/
```

**Готово!** 🎉



