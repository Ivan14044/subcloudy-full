# Полное руководство по деплою Subcloudy

## 📌 Параметры сервера

- **SSH:** `root@193.111.63.104`
- **Frontend Root:** `/var/www/subcloudy/public/`
- **Backend Root:** `/var/www/subcloudy/ai-bot-main/`

---

## 🛠 Шаг 1: Подготовка и сборка (Локально)

### Frontend (Vue 3)
```bash
cd ai-bot-front-main
npm install
npm run build
```

### Backend (Laravel) - если были изменения
```bash
cd ai-bot-main
composer install --no-dev --optimize-autoloader
```

---

## 🚀 Шаг 2: Загрузка на сервер (SCP)

### Загрузка Frontend (Production build)
```bash
# Основная директория фронтенда
scp -r ai-bot-front-main/dist/* root@193.111.63.104:/var/www/subcloudy/public/

# Директория публичных файлов бэкенда (важно для корректных путей)
scp -r ai-bot-front-main/dist/* root@193.111.63.104:/var/www/subcloudy/ai-bot-main/public/
```

### Загрузка Backend
```bash
scp -r ai-bot-main/* root@193.111.63.104:/var/www/subcloudy/ai-bot-main/
```

---

## ⚙️ Шаг 3: Настройка на сервере (SSH)

```bash
ssh root@193.111.63.104
```

### Настройка Backend
```bash
cd /var/www/subcloudy/ai-bot-main

# Права доступа
chmod -R 775 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache

# Кэширование Laravel
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### Перезапуск сервисов
```bash
sudo systemctl restart nginx
sudo systemctl restart php8.1-fpm
```

---

## 📈 Оптимизация SEO (PageSpeed)

При разработке фронтенда следуйте этим правилам:
1. **Ссылки (`<a>`):** Обязательно наличие атрибута `href`. Даже если используется навигация через Vue Router (`router.push`), добавьте `href` для поисковых роботов.
2. **Кнопки (`<button>`):** Используйте для всех интерактивных элементов, которые не являются ссылками (открытие попапов, отправка форм, авторизация через соцсети).
3. **Изображения:** Всегда добавляйте атрибут `alt`.

---

## 📂 Структура проекта на сервере

```text
/var/www/subcloudy/
├── public/              # Точка входа для Nginx (фронтенд)
│   ├── assets/          # JS/CSS бандлы
│   └── index.html       # Главная страница SPA
└── ai-bot-main/         # Laravel Backend
    ├── public/          # Дубликат фронтенда и API entry point
    ├── storage/         # Логи и загруженные файлы
    └── .env             # Настройки окружения
```
