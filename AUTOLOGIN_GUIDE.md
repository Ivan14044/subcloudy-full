# 🔐 Руководство по настройке автологина

## ✅ Что реализовано:

**Desktop-приложение теперь поддерживает автоматический вход через cookies!**

Когда администратор добавляет ServiceAccount с cookies, пользователи в desktop-приложении будут автоматически авторизованы.

---

## 📋 Как это работает:

```
┌─────────────────────────────────────┐
│ 1. Admin добавляет ServiceAccount   │
│    с cookies в БД                   │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 2. User открывает сервис            │
│    в Desktop App                    │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 3. Backend назначает account        │
│    и возвращает cookies             │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 4. Desktop App загружает cookies    │
│    в изолированную Electron session │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 5. Сервис открывается               │
│    УЖЕ АВТОРИЗОВАННЫМ! ✅           │
└─────────────────────────────────────┘
```

---

## 👨‍💼 Инструкция для Администратора

### **Шаг 1: Экспорт cookies из авторизованного аккаунта**

#### **Способ A: Через расширение браузера (рекомендуется)**

1. **Установите расширение для экспорта cookies:**
   - Chrome/Edge: [EditThisCookie](https://chrome.google.com/webstore/detail/editthiscookie/fngmhnnpilhplaeedifhccceomclgfbg)
   - Chrome/Edge: [Cookie Editor](https://chrome.google.com/webstore/detail/cookie-editor/hlkenndednhfkekhgcdicdfddnkalmdm)
   - Firefox: [Cookie Quick Manager](https://addons.mozilla.org/firefox/addon/cookie-quick-manager/)

2. **Авторизуйтесь в сервисе:**
   - Откройте https://chatgpt.com (или другой сервис)
   - Войдите в premium аккаунт
   - Убедитесь что всё работает

3. **Экспортируйте cookies:**
   - Кликните на иконку расширения
   - Нажмите "Export" → "JSON"
   - Скопируйте весь JSON

**Пример экспортированных cookies:**
```json
[
  {
    "domain": ".chatgpt.com",
    "expirationDate": 1735689600,
    "hostOnly": false,
    "httpOnly": true,
    "name": "__Secure-next-auth.session-token",
    "path": "/",
    "sameSite": "lax",
    "secure": true,
    "session": false,
    "storeId": "0",
    "value": "eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2R0NNIn0..."
  },
  {
    "domain": ".chatgpt.com",
    "hostOnly": false,
    "httpOnly": false,
    "name": "_dd_s",
    "path": "/",
    "sameSite": "strict",
    "secure": true,
    "session": true,
    "value": "rum=0&expire=1733162340815"
  }
  // ... остальные cookies
]
```

#### **Способ B: Через DevTools (для продвинутых)**

1. Откройте DevTools (F12)
2. Перейдите в **Application** → **Cookies**
3. Скопируйте все cookies вручную

---

### **Шаг 2: Создание ServiceAccount в admin-панели**

1. **Перейдите в admin-панель Laravel:**
   ```
   http://127.0.0.1:8000/admin/service-accounts/create
   ```

2. **Заполните форму:**
   - **Service:** Выберите сервис (например, ChatGPT)
   - **Profile ID:** Уникальный ID (например: `chatgpt-premium-001`)
   - **Is Active:** ✅ Включено
   - **Credentials:** Вставьте JSON:

```json
{
  "cookies": [
    {
      "name": "__Secure-next-auth.session-token",
      "value": "eyJhbGciOiJkaXIi...",
      "domain": ".chatgpt.com",
      "path": "/",
      "secure": true,
      "httpOnly": true,
      "sameSite": "lax",
      "expirationDate": 1735689600
    }
    // ... вставьте ВСЕ экспортированные cookies
  ],
  "email": "premium@account.com"
}
```

3. **Сохраните**

---

### **Шаг 3: Тестирование**

1. **Откройте Desktop-приложение**
2. **Войдите как пользователь** с активной подпиской на этот сервис
3. **Кликните "Open Service"**
4. **Сервис должен открыться УЖЕ АВТОРИЗОВАННЫМ!** ✅

---

## 🔄 **Для каждого сервиса:**

### **ChatGPT:**
```json
{
  "cookies": [
    {"name": "__Secure-next-auth.session-token", "value": "...", "domain": ".chatgpt.com"},
    {"name": "__Secure-next-auth.callback-url", "value": "...", "domain": ".chatgpt.com"}
  ]
}
```

### **Midjourney:**
```json
{
  "cookies": [
    {"name": "__Secure-next-auth.session-token", "value": "...", "domain": ".midjourney.com"},
    {"name": "connect.sid", "value": "...", "domain": ".midjourney.com"}
  ]
}
```

### **Canva:**
```json
{
  "cookies": [
    {"name": "canva_session", "value": "...", "domain": ".canva.com"},
    {"name": "gtm_auth_staging", "value": "...", "domain": ".canva.com"}
  ]
}
```

---

## 🛡️ **Безопасность:**

### **В БД (Laravel):**

Credentials шифруются автоматически:

```php
// В модели ServiceAccount
protected $casts = [
    'credentials' => 'encrypted:array' // Laravel шифрует автоматически
];
```

### **В Desktop App:**

```typescript
// Cookies загружаются ТОЛЬКО в изолированную session
// После закрытия окна - session.clearStorageData()
// Пользователь НИКОГДА не видит cookies
// DevTools заблокированы - извлечь cookies невозможно
```

---

## 📊 **Структура таблицы service_accounts:**

```sql
CREATE TABLE service_accounts (
    id BIGINT PRIMARY KEY,
    service_id BIGINT,              -- ID сервиса
    profile_id VARCHAR(255),        -- Уникальный ID профиля
    credentials TEXT,               -- JSON с cookies (зашифрован)
    is_active BOOLEAN DEFAULT 1,   -- Активен ли аккаунт
    used INT DEFAULT 0,             -- Сколько раз использован
    expiring_at TIMESTAMP NULL,     -- Когда истекает
    last_used_at TIMESTAMP NULL,    -- Последнее использование
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

---

## 💡 **Лучшие практики:**

### **1. Уникальные profile_id:**
```
chatgpt-premium-001
chatgpt-premium-002
midjourney-pro-001
canva-enterprise-001
```

### **2. Экспорт ВСЕХ cookies:**
Не только session token, но и все cookies для домена:
- Authentication cookies
- Session cookies
- Tracking cookies (могут влиять на авторизацию)
- CSRF tokens

### **3. Регулярное обновление:**
```
- Cookies истекают (expirationDate)
- Проверяйте is_active
- Обновляйте cookies раз в неделю/месяц
```

### **4. Мониторинг:**
```sql
-- Проверить когда аккаунт последний раз использовался
SELECT profile_id, last_used_at, used 
FROM service_accounts 
WHERE service_id = 1 
ORDER BY last_used_at DESC;

-- Проверить истекшие аккаунты
SELECT profile_id, expiring_at 
FROM service_accounts 
WHERE expiring_at < NOW();
```

---

## 🧪 **Тестирование:**

### **1. Проверка что cookies работают:**

```bash
# В DevTools приложения (Console):
document.cookie
# Должны быть cookies загружены
```

### **2. Проверка автологина:**

1. Откройте сервис
2. Проверьте что НЕ показывается страница логина
3. Должен быть сразу главный экран (dashboard)

### **3. Проверка изоляции:**

1. Откройте один сервис
2. Откройте другой сервис
3. Они должны иметь разные сессии (разные аккаунты)

---

## ⚠️ **Важные замечания:**

### **Cookies должны включать:**

Для **ChatGPT** обязательные cookies:
- `__Secure-next-auth.session-token` ← САМЫЙ ВАЖНЫЙ
- `__Secure-next-auth.callback-url`
- `ajs_user_id`
- `ajs_anonymous_id`

Для **других сервисов** - проверьте какие cookies критичны для авторизации.

### **Проблемы и решения:**

**Проблема:** Сервис просит войти заново  
**Решение:** Проверьте что экспортировали ВСЕ cookies, не только session

**Проблема:** Cookies истекли  
**Решение:** Обновите ServiceAccount с новыми cookies

**Проблема:** Account заблокирован сервисом  
**Решение:** Создайте новый premium аккаунт и экспортируйте cookies

---

## 📝 **Пример заполнения формы admin-панели:**

```
Service: ChatGPT Plus
Profile ID: chatgpt-premium-001
Is Active: ✅ Yes

Credentials (JSON):
{
  "cookies": [
    {
      "name": "__Secure-next-auth.session-token",
      "value": "eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2R0NNIn0.8fN3LKmHVZCTqQ4sWx0jPA.full_token_here...",
      "domain": ".chatgpt.com",
      "path": "/",
      "secure": true,
      "httpOnly": true,
      "sameSite": "lax",
      "expirationDate": 1735689600
    },
    {
      "name": "__Secure-next-auth.callback-url",
      "value": "https%3A%2F%2Fchatgpt.com",
      "domain": ".chatgpt.com",
      "path": "/",
      "secure": true,
      "httpOnly": true,
      "sameSite": "lax",
      "expirationDate": 1735689600
    }
  ],
  "email": "premium@chatgpt.account.com"
}
```

**ВАЖНО:** Вставляйте полный массив cookies, не обрезайте!

---

## 🎯 **Результат:**

После правильной настройки:
- ✅ Admin добавляет account с cookies
- ✅ User кликает "Open Service"  
- ✅ Desktop App загружает cookies
- ✅ **Сервис открывается авторизованным!**
- ✅ User сразу может работать
- ✅ Cookies защищены (DevTools заблокированы)

---

## 🚀 **Статус реализации:**

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Backend (DesktopController) | ✅ | Возвращает cookies |
| Desktop (ServiceManager) | ✅ | Загружает cookies в session |
| Desktop (ServiceWindow) | ✅ | Использует session с cookies |
| Безопасность | ✅ | Cookies защищены от извлечения |

**Автологин полностью реализован!** 🎉

---

## 📞 Следующие шаги:

1. **Запустите приложение** (уже запущено)
2. **Добавьте тестовый ServiceAccount** с cookies
3. **Войдите как пользователь** с подпиской
4. **Кликните "Open Service"**
5. **Сервис должен открыться авторизованным!** ✅

**Проверьте логи в терминале - там будет информация о загрузке cookies!**

