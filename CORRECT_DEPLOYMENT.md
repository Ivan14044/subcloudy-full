# ❌ ПРОБЛЕМА: Неправильный процесс деплоя

## Что было неправильно:

1. **Неправильная директория**: Деплоил в `/var/www/html/`, но Nginx настроен на `/var/www/subcloudy/backend/public/`
2. **Нет резервной копии**: Удалял файлы без бэкапа
3. **Нет атомарного деплоя**: Простое удаление и копирование может привести к downtime
4. **Нет проверки**: Не проверял работоспособность после деплоя

## ✅ Правильный процесс:

### Структура на сервере:
- **Исходники**: `/var/www/subcloudy/ai-bot-front-main/` (где собирается проект)
- **Публичная директория**: `/var/www/subcloudy/backend/public/` (откуда Nginx отдает файлы)
- **Nginx root**: `root /var/www/subcloudy/backend/public;`

### Правильный процесс деплоя:

1. **Создать резервную копию** текущей версии
2. **Собрать проект** (локально или на сервере)
3. **Атомарно заменить** файлы (минимальный downtime)
4. **Проверить** работоспособность
5. **Откатить** при проблемах

## Нужно исправить:

Сейчас в правильной директории `/var/www/subcloudy/backend/public/` находится СТАРАЯ версия (`index-yT2jmpfd.js`).

Нужно задеплоить новую версию в правильную директорию!

