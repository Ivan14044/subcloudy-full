# 🔧 Исправление запуска сервисов

## 🐛 Проблема:

При нажатии на "Open Service" происходит timeout при попытке подключения к `https://workspace.subcloudy.com/api/new`

**Ошибка:**
```
ERR_CONNECTION_TIMED_OUT
URL: https://workspace.subcloudy.com/api/new?token=...
```

---

## 💡 Причина:

Внешний **Browser API** (workspace.subcloudy.com) недоступен или не отвечает. Это сервис который управляет изолированными браузерными сессиями.

---

## ✅ Что исправлено:

### 1. **Logout ошибка (405 Method Not Allowed)**

**Было:** `POST /api/logout`  
**Стало:** `GET /api/logout`

### 2. **Добавлен fallback для запуска сервиса**

Если Browser API недоступен, приложение теперь:
1. Пытается загрузить через Browser API
2. Если timeout - загружает прямой URL сервиса (app_url)

```typescript
try {
  await view.webContents.loadURL(browserApiUrl);
} catch (error) {
  if (error.code === 'ERR_CONNECTION_TIMED_OUT') {
    // Fallback: загружаем прямой URL
    await view.webContents.loadURL(directServiceUrl);
  }
}
```

---

## 🎯 Варианты решения:

### **Вариант 1: Запустить Browser API (рекомендуется для production)**

Browser API - это отдельный сервис, который должен работать на `workspace.subcloudy.com`.

Если у вас есть доступ к этому сервису:
1. Запустите его
2. Убедитесь что он доступен
3. Перезапустите desktop приложение

### **Вариант 2: Использовать прямые URL (для тестирования)**

Сервис будет открываться напрямую по URL (например ChatGPT.com), но:
- ❌ Без автологина
- ❌ Без изоляции профилей
- ✅ Но DevTools всё равно заблокированы
- ✅ Watermark работает

### **Вариант 3: Локальный Browser API**

Если Browser API есть локально:
1. Измените URL в backend config
2. Или запустите локальную версию

---

## 🔍 Что проверить:

### **1. Browser API доступен?**

Откройте в браузере:
```
https://workspace.subcloudy.com/api/new
```

Должен вернуть ответ (может быть ошибка, но НЕ timeout).

### **2. Backend правильно настроен?**

Файл: `ai-bot-main/config/services.php`

```php
'browser_api' => [
    'url' => env('BROWSER_API_URL', 'https://workspace.subcloudy.com/api/'),
],
```

### **3. Проверить endpoint desktop/service-url**

```bash
# С вашим токеном
curl http://127.0.0.1:8000/api/desktop/service-url \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"service_id": 1}'
```

Должен вернуть:
```json
{
  "url": "https://workspace.subcloudy.com/api/new",
  "token": "...",
  "profile_id": "...",
  "app_url": "https://chatgpt.com/..."
}
```

---

## 💡 Временное решение (для тестирования):

Можно изменить `DesktopController.php` чтобы он возвращал прямой URL без Browser API:

```php
public function getSecureServiceUrl(Request $request)
{
    $serviceId = $request->service_id;
    $service = Service::findOrFail($serviceId);
    
    // Временно: возвращаем прямой URL
    $appUrl = $service->params['link'] ?? 'https://google.com';
    
    return response()->json([
        'success' => true,
        'url' => $appUrl, // Прямой URL вместо Browser API
        'token' => '',
        'profile_id' => '',
        'app_url' => $appUrl
    ]);
}
```

Тогда сервис откроется, но без автологина и профилей.

---

## 🚀 Текущий статус:

- ✅ Дизайн на 100% как на сайте
- ✅ Авторизация работает
- ✅ Список сервисов отображается
- ⚠️ Запуск сервиса - нужен доступ к Browser API

---

## 📝 Следующий шаг:

**Выберите один из вариантов:**

1. **Запустить/настроить Browser API** - для полной функциональности
2. **Использовать fallback** - текущая версия уже пробует прямой URL
3. **Изменить backend** - чтобы возвращал прямые URL для desktop

**Проверьте логи в DevTools после клика на "Open Service" - там будет детальная информация!**


