# 🍪 Импорт cookies в админ-панели - Готово!

## ✅ Что добавлено:

В админ-панель добавлено **удобное поле для импорта cookies** с автоматической валидацией!

---

## 📍 Где найти:

### **Создание нового аккаунта:**
```
http://127.0.0.1:8000/admin/service-accounts/create
```

### **Редактирование существующего:**
```
http://127.0.0.1:8000/admin/service-accounts/edit/{id}
```

---

## 🎯 Как использовать (5 шагов):

### **Шаг 1: Установите расширение**

Откройте Chrome Web Store:
```
https://chrome.google.com/webstore/detail/editthiscookie/fngmhnnpilhplaeedifhccceomclgfbg
```

Нажмите "Добавить в Chrome"

### **Шаг 2: Залогиньтесь в сервис**

1. Откройте сервис (например https://chatgpt.com)
2. Войдите в **premium аккаунт**
3. Убедитесь что работают premium функции

### **Шаг 3: Экспортируйте cookies**

1. Кликните на иконку **EditThisCookie** (🍪) в панели расширений
2. Нажмите кнопку **"Export"** (внизу)
3. **Скопируйте весь JSON** (Ctrl+A, Ctrl+C)

Должно выглядеть примерно так:
```json
[
  {
    "domain": ".chatgpt.com",
    "expirationDate": 1735689600,
    "hostOnly": false,
    "httpOnly": true,
    "name": "__Secure-next-auth.session-token",
    "path": "/",
    "sameSite": "lax",
    "secure": true,
    "value": "eyJhbGciOiJkaXIi..."
  },
  ...
]
```

### **Шаг 4: Откройте админ-панель**

1. Перейдите: http://127.0.0.1:8000/admin/service-accounts/create

2. Заполните форму:
   - **Service:** Выберите сервис (ChatGPT)
   - **Status:** Active
   - **Profile ID:** Нажмите "Start Browser" (или введите вручную: `chatgpt-premium-001`)

3. **Вставьте cookies:**
   - Найдите поле **"Cookies Import (JSON)"**
   - **Вставьте** скопированный JSON (Ctrl+V)
   - Кликните вне поля (blur)

4. **Проверьте валидацию:**
   - Должно появиться: ✅ **"Найдено 15 cookie(s). Формат корректен!"**
   - Если ошибка - проверьте формат JSON

### **Шаг 5: Сохраните**

1. Опционально заполните Email (для справки)
2. Нажмите **"Create"**
3. Готово! ✅

---

## 🖥️ **Что видит администратор:**

### **В форме:**

```
┌──────────────────────────────────────────┐
│ Service: [ChatGPT ▼]                     │
│ Status: [Active ▼]                       │
│ Profile ID: chatgpt-premium-001          │
├──────────────────────────────────────────┤
│ 💡 Для автоматического входа:            │
│ 1. Установите EditThisCookie             │
│ 2. Войдите в premium аккаунт             │
│ 3. Экспортируйте cookies                 │
│ 4. Вставьте JSON ниже                    │
├──────────────────────────────────────────┤
│ Cookies Import (JSON):                   │
│ ┌────────────────────────────────────┐   │
│ │ [{"name":"__Secure-next-auth...    │   │
│ │   "value":"eyJhbGci...",           │   │
│ │   "domain":".chatgpt.com",         │   │
│ │   "secure":true}]                  │   │
│ └────────────────────────────────────┘   │
│ ✅ Найдено 15 cookie(s). Формат корректен!│
├──────────────────────────────────────────┤
│ Email: premium@chatgpt.com (опц.)        │
│ Password: (опц.)                         │
├──────────────────────────────────────────┤
│ [Create] [Cancel]                        │
└──────────────────────────────────────────┘
```

---

## ✨ Функции поля импорта:

### **1. Автоматическая валидация:**
- При потере фокуса (blur) - проверяет JSON
- Показывает количество найденных cookies
- Показывает примеры первых 3 cookies

### **2. Визуальная обратная связь:**
```
✅ Найдено 15 cookie(s). Формат корректен!
Примеры:
- __Secure-next-auth.session-token для домена .chatgpt.com
- __Secure-next-auth.callback-url для домена .chatgpt.com
- ajs_user_id для домена .chatgpt.com
```

### **3. Обработка ошибок:**
```
❌ Ошибка формата: Unexpected token } in JSON
```

### **4. Автоматическое сохранение:**
- При нажатии "Create/Save" cookies автоматически добавляются в `credentials[cookies]`
- Сохраняются в зашифрованном виде в БД

---

## 📊 Структура после сохранения:

В БД в поле `credentials` будет:

```json
{
  "cookies": [
    {
      "name": "__Secure-next-auth.session-token",
      "value": "полный_токен",
      "domain": ".chatgpt.com",
      "path": "/",
      "secure": true,
      "httpOnly": true,
      "sameSite": "lax",
      "expirationDate": 1735689600
    }
    // ... все остальные cookies
  ],
  "email": "premium@chatgpt.com",
  "password": null
}
```

---

## 🧪 Тестирование:

### **1. Создайте тестовый ServiceAccount:**

1. Откройте: http://127.0.0.1:8000/admin/service-accounts/create
2. Выберите сервис
3. Вставьте cookies (даже тестовые):
```json
[
  {
    "name": "test_cookie",
    "value": "test_value",
    "domain": ".chatgpt.com",
    "path": "/",
    "secure": true
  }
]
```
4. Сохраните

### **2. Проверьте в БД:**

```sql
SELECT id, profile_id, credentials 
FROM service_accounts 
WHERE id = (SELECT MAX(id) FROM service_accounts);
```

Должно быть:
```json
credentials: {
  "cookies": [{"name":"test_cookie",...}],
  "email": "..."
}
```

### **3. Протестируйте в Desktop App:**

1. Откройте Desktop App
2. Войдите как пользователь с подпиской
3. Кликните "Open Service"
4. Проверьте логи:

```
[Services] Account data received: { cookies_count: 1 }
[Services] Loading cookies into session...
[Services] Cookie loaded: test_cookie for domain: .chatgpt.com
[Services] Cookies loaded: 1 success, 0 failed
```

**Если видите успех - всё работает!** ✅

---

## ⚠️ Важные замечания:

### **1. Экспортируйте ВСЕ cookies:**

Не только session token! Многие сервисы используют несколько cookies:
- Session tokens
- CSRF tokens
- Tracking cookies (могут влиять на авторизацию)
- User preferences

### **2. Формат JSON должен быть валидным:**

✅ **Правильно:**
```json
[{"name":"cookie1","value":"value1","domain":".example.com"}]
```

❌ **Неправильно:**
```json
{"name":"cookie1","value":"value1"}  // Не массив!
[{name:"cookie1","value":"value1"}]   // Кавычки обязательны!
```

### **3. Обновляйте периодически:**

Cookies истекают! Проверяйте `expirationDate` и обновляйте когда нужно.

---

## 📝 Чек-лист администратора:

### **При создании ServiceAccount:**
- [ ] Сервис выбран правильно
- [ ] Profile ID уникальный
- [ ] Cookies экспортированы из АВТОРИЗОВАННОГО аккаунта
- [ ] JSON валидный (видна зеленая галочка ✅)
- [ ] Email добавлен (для справки)
- [ ] is_active = true
- [ ] Сохранено

### **После сохранения:**
- [ ] Проверен в БД (есть credentials.cookies)
- [ ] Протестирован в Desktop App
- [ ] Сервис открывается авторизованным

---

## 🎊 Готово!

**Теперь администратор может:**
- ✅ Легко импортировать cookies через удобное поле
- ✅ Видеть валидацию в реальном времени
- ✅ Проверять количество cookies
- ✅ Сохранять одним кликом

**Desktop-приложение автоматически:**
- ✅ Получает cookies с backend
- ✅ Загружает в изолированную session
- ✅ Открывает сервис авторизованным

**Попробуйте прямо сейчас!** 🚀

1. Откройте http://127.0.0.1:8000/admin/service-accounts/create
2. Вставьте любые тестовые cookies
3. Сохраните
4. Откройте Desktop App и протестируйте!

