# 🚀 Инструкция по деплою на сервер (Актуальная версия)

## 📌 Основные параметры сервера

- **IP сервера:** `193.111.63.104`
- **Пользователь:** `root`
- **Пароль:** `HJjuUJ73j0qE18hF2w`
- **Корневая директория сайта (Frontend):** `/var/www/subcloudy/public/`
- **Директория Backend (Laravel):** `/var/www/subcloudy/ai-bot-main/`

---

## 🛠 Процесс деплоя

### 1️⃣ Сборка Frontend локально
Перед загрузкой на сервер необходимо собрать проект на вашем компьютере:

```bash
cd D:\project\Subcloudy\ai-bot-front-main
npm install
npm run build
```

### 2️⃣ Загрузка файлов на сервер
**ВАЖНО:** Файлы фронтенда должны быть загружены в директорию `/var/www/subcloudy/public/`. Также рекомендуется обновить их в `/var/www/subcloudy/ai-bot-main/public/` для корректной работы фоллбэков.

```bash
# Загрузка фронтенда (директория dist)
scp -r D:\project\Subcloudy\ai-bot-front-main\dist\* root@193.111.63.104:/var/www/subcloudy/public/

# Загрузка фронтенда в папку бэкенда (для SEO и роутинга)
scp -r D:\project\Subcloudy\ai-bot-front-main\dist\* root@193.111.63.104:/var/www/subcloudy/ai-bot-main/public/
```

### 3️⃣ Настройка прав и перезапуск сервисов
После загрузки файлов необходимо установить права и перезапустить Nginx:

```bash
ssh root@193.111.63.104 "chown -R www-data:www-data /var/www/subcloudy/public && systemctl restart nginx"
```

---

## 🔍 Оптимизация SEO и исправление ошибок PageSpeed

### Исправленные проблемы:
1. **Ссылки без href:** В Vue-компонентах ссылки, работающие только через `@click`, заменены на стандартные ссылки с `href` для корректного сканирования поисковыми роботами.
2. **Сканирование кнопок:** Элементы, открывающие модальные окна или выполняющие действия (например, вход через соцсети), заменены на теги `<button>`, чтобы Google не пытался сканировать их как страницы.

### Рекомендации по разработке:
- Всегда добавляйте `href` в теги `<a>`, даже если используете `@click.prevent`.
- Используйте `<button>` для действий, которые не ведут на новую страницу.
- Проверяйте сайт через [PageSpeed Insights](https://pagespeed.web.dev/) после каждого крупного обновления.

---

## 🔄 Решение проблем

- **Если изменения не видны:** Перезапустите Nginx (`systemctl restart nginx`) и проверьте путь `/var/www/subcloudy/public/index.html`.
- **Ошибка 404 на страницах:** Убедитесь, что в конфиге Nginx прописано `try_files $uri $uri/ /index.html;`.
- **Проблемы с API:** Проверьте логи Laravel в `/var/www/subcloudy/ai-bot-main/storage/logs/laravel.log`.
